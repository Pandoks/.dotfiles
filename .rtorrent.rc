directory.default.set = ~/rtorrent/media

session.path.set = ~/.rtorrent/

# Local SCGI/XMLRPC socket so external tools can control rtorrent.
# Unix socket only (not exposed on the network); restart rtorrent to apply.
network.scgi.open_local = ~/.rtorrent/rpc.socket
